function ce=calculateError(disTestDataset, featureIndex, classIndex, prior, likelihood)
   misclass=0;
   for i=1:size(disTestDataset,1)
      k0=prior(1)*likelihood(1,disTestDataset(i, featureIndex));
      k1=prior(2)*likelihood(2,disTestDataset(i, featureIndex));
      if K0>k1
        dc=0;
      else
          dc=1;
      end
      disTestDataset)
    end
end